{
    "name": "Chorus",
    "version": "0.1.0",
    "engines": {
        "node": "8.1.2",
        "yarn": "0.24.6"
    },
    "scripts": {
        "heroku-postbuild": "cd client && yarn install && yarn build:prod"
    }
}
